sudo: required
services:
  - docker

before_install:
  - docker build -t johnnyrepo/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true johnnyrepo/docker-react npm run test -- --coverage